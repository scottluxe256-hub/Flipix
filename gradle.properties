# Mengaktifkan AndroidX
android.useAndroidX=true

# Matikan Jetifier agar Gradle tidak mutasi dependency saat build
android.enableJetifier=false

# Alokasi memori JVM
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
